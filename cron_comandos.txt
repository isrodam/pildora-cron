📘 ENCICLOPEDIA TÉCNICA DE CRON: COMANDOS Y CONCEPTOS

1. Gestión de la "Agenda" (El comando crontab)

Es el archivo donde el usuario guarda sus instrucciones.
•	crontab -e: Editar. Abre el editor de texto para añadir o modificar tareas.
•	crontab -l: Listar. Muestra por pantalla las tareas programadas actualmente.
•	crontab -r: Eliminar (Remove). Borra todas las tareas del usuario.
•	crontab -i: Interactivo. Igual que -r, pero pide confirmación antes de borrar (seguridad).
•	crontab -u [usuario] -l: Permite ver o editar la agenda de otro usuario (requiere sudo).

2. Gestión del "Motor" (Servicio del Sistema)

Sin el motor (demonio) encendido, la agenda no se ejecuta.
•	sudo service cron start: Arranca el motor.
•	sudo service cron stop: Apaga el motor.
•	sudo service cron status: El más importante. Muestra si está active (running).
•	sudo service cron restart: Reinicia el servicio para refrescar configuraciones.

3. Inspección de Procesos y Sistema (Nivel Experto)

Para ver qué está pasando "detrás de las cámaras" en Linux.
•	ps aux | grep cron: Muestra si el proceso de Cron está cargado en la memoria RAM.
•	pgrep cron: Solo te da el ID del proceso si Cron está funcionando.
•	whereis python3: Te da la ruta absoluta de Python (necesaria para el cronjob).
•	which python3: Similar al anterior, te da la ruta exacta que usa el sistema por defecto.

4. Auditoría y Depuración (Logs del Sistema)

Cuando tu log personal falla, hay que mirar el diario general de Linux.
•	sudo tail -f /var/log/syslog: Diario general de eventos de Linux. Aquí Cron escribe cada vez que intenta ejecutar algo y si ha habido un error de sistema.
•	grep -i cron /var/log/syslog: Filtra el diario del sistema para mostrar solo las entradas relacionadas con Cron.
•	grep -i error /var/log/syslog: Busca específicamente la palabra "error" en el diario.

5. Visores de Archivos (Para ver tus resultados)

Diferentes formas de leer los logs que genera tu script.
•	cat [archivo]: Vuelca todo el contenido de golpe.
•	tail -f [archivo]: Monitorización en tiempo real (se queda abierto).
•	tail -n [X] [archivo]: Muestra solo las últimas X líneas y se cierra.
•	less [archivo]: Permite navegar por el archivo con las flechas (pulsa q para salir). Es el visor profesional.

6. Sintaxis y Atajos de Tiempo

La estructura obligatoria de una tarea: * * * * * [comando].
Atajos especiales (Sustituyen los asteriscos):
•	@reboot: Ejecuta al arrancar el sistema.
•	@daily: Ejecuta una vez al día a medianoche (0 0 * * *).
•	@hourly: Ejecuta una vez por hora (0 * * * *).
•	@weekly: Una vez a la semana.


💡 Conceptos Teóricos
 
•	¿Qué es un Demonio (Daemon)?: Un proceso que se ejecuta en segundo plano sin interfaz de usuario, esperando eventos (como el reloj) para actuar.
•	Rutas Absolutas vs Relativas: Cron no sabe en qué carpeta estás. Siempre hay que usar la ruta desde la raíz (/mnt/c/...).
•	Redirección de Errores (2>&1): Los programas tienen dos salidas: la normal (1) y la de error (2). Al poner 2>&1 al final de tu cronjob, obligas a que los fallos también se escriban en tu archivo .log.
•	Variable de Entorno PATH: Cron tiene un PATH muy limitado (no sabe dónde están todos los programas). Por eso es mejor poner /usr/bin/python3 en lugar de solo python3.

________________________________________

🚀 Comandos y teclas de uso habitual
 
1.	Muestra los resultados con tail -f.
2.	Pulsa Ctrl + C para recuperar la terminal.
3.	Comprueba el estado del motor: sudo service cron status.
4.	Enseña tu agenda configurada: crontab -l.
5.	Limpia todo para terminar: crontab -r.

